# ── Google Cloud ──────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# ── SendGrid ───────────────────────────────────────────
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=noreply@tuodominio.it
FROM_NAME=HomeStager AI

# ── Replicate (opzionale — attiva ControlNet Depth per fedeltà spaziale garantita)
# Senza questo token viene usato Imagen 3 inpainting (buona qualità).
# Con questo token viene usato ControlNet Depth via Replicate (qualità premium).
# Registrati su https://replicate.com e copia il token dall'account.
# REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

allow_origins=[
    "http://localhost:5173",
    "staged-ai-six.vercel.app",
]
